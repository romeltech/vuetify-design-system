{
  "name": "vuetify-system-design",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Monorepo: the vuetify-preset package + its living style-guide playground.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/playground",
    "build": "npm run build --workspace apps/playground",
    "preview": "npm run preview --workspace apps/playground"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  }
}
